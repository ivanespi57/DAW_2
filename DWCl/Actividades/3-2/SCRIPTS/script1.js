function introPalab(){
    let palabras = [];
    let ascii = 
    let agrego = prompt("Agrega palabras al array");

    while(agrego !== null && agrego.trim() !== ""){
        
        palabras.push(agrego);

        alert(palabras);

        for(let i = 0; i <= palabras.lenght(); i++){
            let p = palabras[i];

            if(p > 1 && p < 10){

            }
        }

    }
}